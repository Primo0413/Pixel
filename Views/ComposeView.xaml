<UserControl x:Class="pixel_edit.Views.ComposeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Style="{StaticResource PageRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource SettingsCardStyle}" Padding="14">
                <StackPanel>
                    <TextBlock Text="拼接 / 堆叠" FontSize="22" FontWeight="SemiBold" Foreground="#FF1B1B1B" />

                    <TextBlock Text="组合模式" FontWeight="SemiBold" Margin="0,12,0,4" />
                    <ComboBox ItemsSource="{Binding Modes}" SelectedItem="{Binding SelectedMode}" />

                    <TextBlock Text="已加载工程" FontWeight="SemiBold" Margin="0,16,0,4" />
                    <ListBox ItemsSource="{Binding LoadedProjectNames}" Height="540" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource SettingsCardStyle}" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="组合结果预览信息" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="{Binding ComposedProject.Name, StringFormat=项目：{0}}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding ComposedProject.Canvas.Width, StringFormat=宽：{0}}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding ComposedProject.Canvas.Height, StringFormat=高：{0}}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding ComposedProject.ComposeMode, StringFormat=模式：{0}}" Margin="0,0,0,6" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Style="{StaticResource SettingsCardStyle}"
                Background="{StaticResource ActionBarBackgroundBrush}"
                Padding="12">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="返回首页" Command="{Binding BackHomeCommand}" Style="{StaticResource ActionBarButtonStyle}" />
                    <Button Content="加载本地工程" Command="{Binding AddProjectsCommand}" Style="{StaticResource ActionBarButtonStyle}" />
                    <Button Content="在编辑器打开" Command="{Binding OpenInEditorCommand}" Style="{StaticResource ActionBarButtonStyle}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="导出PNG" Command="{Binding ExportPngCommand}" Style="{StaticResource ActionBarButtonStyle}" />
                    <Button Content="保存组合工程" Command="{Binding SaveComposedProjectCommand}" Style="{StaticResource ActionBarButtonStyle}" />
                    <Button Content="执行组合" Command="{Binding ComposeCommand}" Style="{StaticResource PrimaryActionBarButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>

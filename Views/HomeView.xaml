<UserControl x:Class="pixel_edit.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFFFF" CornerRadius="14" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="欢迎使用 Pixel Edit"
                           Style="{StaticResource MaterialDesignHeadlineSmallTextBlock}" />

                <TextBlock Grid.Row="1"
                           Margin="0,10,0,0"
                           Text="在左侧侧边栏选择功能：加载图片转换、创建白板绘制、或加载本地像素画进行拼接/堆叠。"
                           Opacity="0.78"
                           TextWrapping="Wrap" />

                <Border Grid.Row="2" Margin="0,18,0,0" Background="#FFF7F9FC" CornerRadius="10" Padding="14">
                    <StackPanel>
                        <TextBlock Text="建议流程" FontWeight="SemiBold" />
                        <TextBlock Text="1) 从图片转换或白板开始创作" Margin="0,8,0,0" Opacity="0.82" />
                        <TextBlock Text="2) 保存为 pxproj.json 并导出 PNG" Margin="0,6,0,0" Opacity="0.82" />
                        <TextBlock Text="3) 进入拼接/堆叠页面做组合" Margin="0,6,0,0" Opacity="0.82" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource ActionBarBackgroundBrush}"
                CornerRadius="12"
                Padding="12">
            <DockPanel>
                <TextBlock Text="从左侧选择一个开始动作"
                           VerticalAlignment="Center"
                           Opacity="0.72" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
